<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lazyi.mapper.UserMapper">
    <select id="queryUserList" resultType="User">
         select * from user
    </select>

    <select id="selectUserById" resultType="User">
          select * from user where userId = #{userId}
    </select>

    <insert id = "addUser" parameterType="User">
        insert into user (userId,userName,userPassword) valuse (#{userId},#{userName},#{userPassword})
    </insert>

    <update id = "updateUser" parameterType="User">
        update user set userName=#{userName},userPassword=#{userPassword} where userId = #{userId}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where userId = #{userId}
    </delete>
</mapper>